apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jarvis
spec:
  ingressClassName: nginx
  rules:
    - host: jarvis.api.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jarvis
                port:
                  number: 8000